Link : [BitShifted's ScaledSnake (Python)](https://crackmes.one/crackme/68d5e451224c0ec5dcedc333)

# Function Insight
[`importlib`](https://docs.python.org/3/library/importlib.html) -> provide the implementation of import function inside the python code.

[`reduce`](https://docs.python.org/3/library/functools.html) from function tool -> Apply function of two arguments cumulatively to the items of iterable, from left to right, so as to reduce the iterable to a single value

`globals()` ->  returns a dictionary representing the current global symbol table—containing all global variables and functions defined in the current module or script => This dictionary allows dynamic access to, inspection of, and modification of global variables at runtime using standard dictionary syntax

[`eval()`](https://www.w3schools.com/python/ref_func_eval.asp) -> evaluate the expression if valid execute

`exec()` -> execute

[`getattr()`](https://docs.python.org/3/library/functions.html#getattr) -> Return the value of the named attribute of object. name must be a string


